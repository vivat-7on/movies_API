[project]
name = "auth_service"
version = "0.1.0"
requires-python = ">=3.11"

# зависимости можно оставить в requirements.txt, это ок
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]